# Generated by stoatworks-unraid/scripts/gen-docker.mjs from fleet.json.
# Repo: contourtonist. Edit fleet.json and regenerate — direct edits are overwritten.
#
# Pull the published image by default. Uncomment build: . to build from this
# checkout instead — useful when testing a change before it reaches GHCR.

services:
  contourtonist-demo:
    image: ghcr.io/stoatworks-labs/contourtonist-demo:latest
    # build: .
    container_name: contourtonist-demo
    restart: unless-stopped
    ports:
      - "8509:80"
